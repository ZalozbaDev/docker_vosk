- python error non-writable array
- non-ONNX variant uses 1 CPU core only
- no confidences reported when LM is used
- hard-coded filenames and paths (spell check, ONNX, ARPA)
- docs for ONNX model (matches which wav2vec2 model?)
- Silero VAD model stored internally?
- partial result shall return VAD active/inactive
- why write wav file on EOF?
- still need to instanciate processor if ONNX model is used later?
- resampling support (for Jitsi Meet)
